{
  "_comment": "DALI Agent Dashboard — single source of truth for the UI. Edit this file to add/remove panes, change colours, and customise the dashboard without restarting.",
  "title": "DALI Agent Dashboard",
  "session": "DALI_session",
  "startmas_path": "Examples/advanced/startmas.sh",
  "accent_color": "#4caf50",
  "panes": [
    {
      "_comment": "Infrastructure panes — always shown, in the order listed here.",
      "id": "server",
      "label": "Server (LINDA)",
      "color": "#121212",
      "border": "#555555"
    },
    {
      "id": "user",
      "label": "User Console",
      "color": "#0b180b",
      "border": "#4caf50"
    }
  ],
  "agents": [
    {
      "_comment": "Agent panes — add/remove entries here; the UI hot-reloads every 5 s.",
      "id": "agent1",
      "label": "Agent 1",
      "color": "#0a0a18",
      "border": "#aa88ff"
    },
    {
      "id": "agent2",
      "label": "Agent 2",
      "color": "#05051a",
      "border": "#2277ff"
    }
  ],

  "filtered_lines": [
    "External event preconditions not verified: no DeltaTime",
    "This is updated list:",
    "This is list without duplicates:",
    "This is list of past event:",
    "This event is first events:",
    "Do not arrive all events"
  ]
}
