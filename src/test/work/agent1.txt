% Test agent for DALI framework validation
% Simple agent with basic DALI constructs

% Initial facts
alive.
counter(0).

% Rules
increment_counter :> 
    counter(X), 
    X1 is X + 1,
    retract(counter(X)),
    assert(counter(X1)).

% Events
test_event :< true.

% Actions
test_action :- 
    write('Test action executed'), nl.

% Goals
obtain(test_goal) :> 
    counter(X), 
    X > 5,
    test_action.

